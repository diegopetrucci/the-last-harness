# Upstream-sync inventory

This contributor-only inventory tracks TLH code that is either copied/adapted from upstream projects or intentionally shimmed against Pi internals. Use it before editing those areas, especially during upstream version bumps.

## Scope and exclusions

Included here:

- active copied/adapted code called out by ticket `tlhm-nkh9`
- Pi-sensitive compatibility shims that patch or mirror upstream behavior

Explicitly excluded here:

- ordinary dependency pins by themselves
- generated runtime `scripts/**/*.mjs` outputs that mirror same-named `scripts/**/*.mts` sources, plus generated same-layout `extensions/**/*.js` outputs that mirror authoritative `extensions/**/*.ts` sources; see [local development](local-development.md) for the generation/freshness workflow

## Review cadence

- **Pi-sensitive items**: re-review on every `@earendil-works/pi-coding-agent` version bump.
- **`annotate-git-diff`**: re-review at least quarterly and on upstream package/repo releases affecting the review UI/flow.

## Copied or adapted code

| Item | Local TLH paths | Upstream repo/package path | Reviewed snapshot | TLH divergences | Risk | Review trigger / cadence | Comparison and validation guidance |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Tokens cache-miss logic | `extensions/the-last-harness/tokens-analyzer.ts` | `@earendil-works/pi-coding-agent` → `src/core/cache-stats.ts` | **Originally attested** in code comment: Pi package `0.80.6`; re-reviewed unchanged against `0.84.1` | The `0.84.1` `cache-stats.ts` miss calculation, noise floor, reset handling, and pricing fallback are unchanged. TLH still ports the detector into offline report analysis, reports `idleMs` for diagnostics, and does not emit transcript notices. | Medium: report math can drift from Pi if upstream changes miss heuristics or usage fields. | On every Pi version bump. | Compare the local cache-miss block with upstream `cache-stats.ts`, then run `node --test tests/the-last-harness-tokens-analyzer.test.mjs tests/the-last-harness-tokens-command.test.mjs`. |
| `annotate-git-diff` review flow | `extensions/annotate-git-diff/`<br>`extensions/shared/quiet-glimpse.ts` | `Jonghakseo/pi-extension` → `packages/diff-review` (`@ryan_nookpi/pi-extension-diff-review`), with original inspiration credit to `badlogic/pi-diff-review` | **Historically reconstructed, not originally attested:** upstream commit `2e839268686fbc530a5ab2cef4667d53fdb4b2d5`; `packages/diff-review` tree SHA `bd0701670693372e687ed732e98ec338e60a0fb4`; package version `0.2.5` | TLH renamed the command/package surface, ships it first-party, moved/adapted the quiet Glimpse host into a shared helper, inlines Monaco assets at build time, and has TLH-specific git, prompt, clipboard, window, and watch behavior. TLH also hardens quiet Glimpse protocol-line parsing by silently ignoring valid non-object JSON while preserving valid object-message data. | High: this is a larger adapted codebase with UI, git, process/protocol, and host-integration surface area. | Quarterly and on upstream package/repo releases affecting the review flow. | Compare the upstream package tree at the reconstructed snapshot with the full local derived family, including `extensions/shared/quiet-glimpse.ts`; run `npm run typecheck` and `node --test tests/quiet-glimpse.test.mjs tests/annotate-git-diff-command.test.mjs tests/annotate-git-diff-git.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Package-update notice renderer | `extensions/the-last-harness/package-update-notice.ts` | `@earendil-works/pi-coding-agent` → `src/modes/interactive/interactive-mode.ts` (`showPackageUpdateNotification`) | Re-reviewed against `0.84.1`; method signature, package-list flow, chat container, and render request remain compatible | TLH retains its prototype override and replaces Pi's package-update banner copy with TLH-specific guidance (`tlh update --extensions`). Pi's 0.84 TUI/fullscreen additions do not change this hook's required fields. | Medium: prototype method stability plus internal renderer field and invocation assumptions can drift on upstream interactive-mode changes. | On every Pi version bump. | Compare the upstream method signature/rendering flow before editing the override and run `node --test tests/the-last-harness-extension-package-update-notice.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Model-reference matcher shim | `extensions/the-last-harness/model-visibility.ts` (`findExactModelReferenceMatch`, exact-match patch) | `@earendil-works/pi-coding-agent` → `src/core/model-resolver.ts` (`findExactModelReferenceMatch`), with contracts in `src/modes/interactive/interactive-mode.ts` (`findExactModelMatch`, `getModelCandidates`), `src/core/model-runtime.ts` (`getAvailable`, `getAvailableSnapshot`), and the `src/core/model-registry.ts` compatibility facade | Re-reviewed against `0.84.1`; Pi now searches its cached filtered snapshot, refreshes catalogs/status, then searches the refreshed snapshot | TLH preserves Pi's filtered cached-match priority, then checks the unfiltered cache for an unscoped canonical reference that resolves to a TLH-hidden model, avoiding refresh/UI for known hidden models. After Pi's delegated refresh, TLH retries the unfiltered snapshot so newly discovered hidden canonical models remain resolvable; bare and scoped references remain delegated, while visible, missing, and ambiguous outcomes retain Pi's priority. | High: the private `findExactModelMatch` prototype patch and model-selection contracts can drift with no public extension API guarantee. | On every Pi version bump. | Compare the matcher with `model-resolver.ts` and re-check ModelRuntime availability/snapshot methods, the model-registry facade, and interactive candidate lookup; verify hidden models remain hidden in selectors while canonical and scoped references retain their intended behavior, then run `node --test tests/the-last-harness-model-visibility.test.mjs tests/the-last-harness-extension-static.test.mjs`. |

## Pi-sensitive compatibility shims

| Shim | Local TLH paths | Upstream repo/package path | Reviewed snapshot | TLH divergences | Risk | Review trigger / cadence | Comparison and validation guidance |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Prototype overrides | `extensions/the-last-harness/new-version-notice.ts`<br>`extensions/the-last-harness/package-update-notice.ts` | `@earendil-works/pi-coding-agent` → `src/modes/interactive/interactive-mode.ts` | Re-reviewed against `0.84.1`; both public prototype methods and their call sites remain present with the same argument shapes | TLH retains its no-op `pi update` suppression and package-update rewrite. Pi's 0.84 renderer refactor changes TUI construction but leaves these methods' required `chatContainer`/`ui` interactions intact. | Medium: although both methods appear in the published class declaration, TLH still depends on prototype hook stability plus internal instance-field and invocation assumptions used by the renderer. | On every Pi version bump. | Confirm both methods, their call sites, and renderer instance fields still match; run `node --test tests/the-last-harness-extension-new-version-notice.test.mjs tests/the-last-harness-extension-package-update-notice.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Model-selection persistence scope | `extensions/the-last-harness/model-selection-scope.ts`<br>`extensions/the-last-harness/primary-agent-runtime.ts` | `@earendil-works/pi-coding-agent` → `src/core/settings-manager.ts` (`setDefaultModelAndProvider`, `setDefaultThinkingLevel`), `src/core/agent-session.ts` (`setModel`, `_emitModelSelect`), and `src/modes/interactive/components/model-selector.ts` (`handleSelect`); related upstream issue [`earendil-works/pi#5263`](https://github.com/earendil-works/pi/issues/5263) | Introduced and reviewed against `0.84.1`; the native selector writes the default once before `AgentSession.setModel`, AgentSession updates the active model and writes it again, and unchanged models emit no `model_select` | TLH intercepts default setters only in the isolated profile and combines async-local `ModelSelector.handleSelect` provenance with a live active-model getter to identify the native `/model` / Ctrl+L selector pair without timing assumptions. Claims remain pending across earlier async extension handlers. Reconfirming the active session-only model is a dropped no-op because Pi emits no event for another scope decision; other unchanged-model writes, `/model <exact-name>`, provider-auth/programmatic sets, cycling, and thinking-only changes retain upstream persistence without the scope prompt. Failed single selector writes are bounded and replayed at the next unrelated write or lifecycle boundary. | High: these process-wide prototype shims depend on private `handleSelect`, its callback preserving async context, exact selector/AgentSession setter order, AgentSession updating its active model before its settings write, and unchanged-model event suppression. | On every Pi version bump and any model-selector, model-event, settings-persistence, or provider-auth flow change. | Recheck the selector callback, setter/model-update/event order, duplicate native-selector signature, unchanged-model early return, direct exact-match command path, provider-auth auto-selection, and sequential extension dispatch; run `node --test tests/the-last-harness-model-selection-scope.test.mjs tests/the-last-harness-primary-agent-runtime.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Model visibility filter | `extensions/the-last-harness/model-visibility.ts` | `@earendil-works/pi-coding-agent` → `src/core/model-runtime.ts`, the `src/core/model-registry.ts` compatibility facade, and `src/modes/interactive/interactive-mode.ts` | Re-reviewed against `0.84.1`; `getAvailable` gained optional auth-operation options while `getAvailableSnapshot` remains the cached read used by interactive lookup | TLH still filters async and snapshot availability for user-facing selectors/cycling, preserves original runtime snapshots for internal provider-aware selection, forwards Pi's new `getAvailable` options, preserves Pi's filtered cached-match priority, and recovers hidden canonical matches both before and after delegated refresh. | Medium: drift here affects model lists, refreshed snapshots, exact matching, and primary-agent model selection. | On every Pi version bump. | Compare against upstream model-runtime snapshots, the model-registry compatibility facade, and interactive-mode candidate/lookup behavior, then run `node --test tests/the-last-harness-model-visibility.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Project-trust/resource mirroring | `extensions/the-last-harness/resources.ts` | `@earendil-works/pi-coding-agent` → `src/main.ts`, `src/core/project-trust.ts`, `src/core/resource-loader.ts`, `src/core/settings-manager.ts`, `src/core/package-manager.ts` | Originally mirrored from Pi `0.79.1`; re-reviewed against `0.84.1` | TLH still delegates context loading/trust-sensitive package resolution to Pi, so the upstream `AGENTS.override.md`-before-`AGENTS.md`/`CLAUDE.md` precedence is inherited without a duplicate loader. **Retained `0.84.1` divergence (low):** Pi's interactive context summary includes file-backed `SYSTEM.md`/`APPEND_SYSTEM.md` sources through resource-loader accessors; TLH's startup `Context:` line intentionally continues to report only `loadProjectContextFiles()`. Focused coverage proves override precedence and ancestor inheritance. | Medium: trust drift could expose or hide the wrong startup resources. | On every Pi version bump. | Compare trust resolution, context-file precedence, and resource filtering assumptions against current Pi code, then run `node --test tests/the-last-harness-resources.test.mjs tests/the-last-harness-header-warning.test.mjs tests/the-last-harness-header-wrap.test.mjs`. |
| Private settings lock | `extensions/the-last-harness/profile-state.ts` (`getSettingsStorageForWrite`, `withLockedTlhSettingsWrite`, `assertSafeTlhSettingsPath`) | `@earendil-works/pi-coding-agent` → `src/core/settings-manager.ts` (`SettingsManager.storage`, `FileSettingsStorage.withLock`) | Re-reviewed against `0.84.1`; private storage property and synchronous `withLock("global", ...)` contract remain unchanged | TLH continues to layer isolated-profile-only write guards, backups, and symlink/hardlink checks around Pi's storage lock; no speculative migration to another settings API is needed. | Medium: the private storage property/lock contract or settings-path layout can change upstream and invalidate TLH's safety assumptions. | On every Pi version bump. | Recheck `SettingsManager.storage` and `withLock("global", ...)` behavior before editing; run `node --test tests/the-last-harness-primary-agent-runtime.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Thinking-level cast | `extensions/the-last-harness/thinking.ts` | `@earendil-works/pi-coding-agent` → extension API thinking-level typings/runtime in `src/core/extensions/types.ts`, `src/core/agent-session.ts`, and `src/main.ts` | Re-reviewed against `0.84.1`; `ExtensionAPI.setThinkingLevel` still accepts TLH's levels, including the legacy `max` path | Legacy compatibility cast retained without behavior change as explicitly required; it remains a separately scoped cleanup candidate rather than opportunistic upgrade work. | Low: stale compatibility code can obscure the real type contract even though this cast does not change runtime behavior. | Re-evaluate on the next Pi bump. | Compare the published ExtensionAPI parameter type with TLH's `ThinkingLevel`; remove the cast in a separately scoped behavior/cleanup ticket if redundant, and run `node --test tests/the-last-harness-extension-static.test.mjs`. |
| Subscription usage compatibility facade | `extensions/the-last-harness/subscription-usage.ts`<br>`extensions/the-last-harness/subscription-usage-facade.ts`<br>`extensions/the-last-harness/footer-subscription-usage.ts` | `@earendil-works/pi-coding-agent` → `src/core/model-registry.ts` / `ModelRuntime` auth facade | Re-reviewed against `0.84.1`; the facade still exposes `isUsingOAuth`, `getApiKeyForProvider`, and `getProviderAuthStatus`, while direct `authStorage` remains an optional legacy path | TLH intentionally keeps its provider-specific usage endpoints, token-fingerprint/account cache isolation, runtime-credential suppression, and optional legacy credential fallback. It does not adopt Pi's unrelated provider-refresh/auth operation APIs because no current TLH use case needs them. | Medium: provider auth shape and subscription eligibility semantics are upstream-sensitive; stale usage must not be shown after credential changes. | On every Pi version bump and provider-auth contract change. | Recheck the published ModelRegistry/ModelRuntime auth methods and run `node --test tests/the-last-harness-subscription-usage.test.mjs tests/the-last-harness-extension-usage-refresh.test.mjs tests/the-last-harness-footer-usage.test.mjs`. |
| Context-cap model mutation | `extensions/the-last-harness/context-cap.ts` | `@earendil-works/pi-coding-agent` → `ExtensionContext.model`, `modelRegistry.getAll()`, and `model_select`/`session_start`/`session_shutdown` events | Re-reviewed against `0.84.1`; model `contextWindow` and lifecycle event contracts remain usable, with no dedicated upstream cap API | TLH keeps its 200k effective cap as a reversible WeakMap-backed mutation across registry models and the active model, restoring windows on shutdown; it remains intentionally independent of Pi's compaction internals. | Medium: model-object identity and lifecycle ordering can drift, leaving a cap applied or restored incorrectly. | On every Pi version bump and context/compaction API change. | Recheck ExtensionContext model and lifecycle event shapes, then run `node --test tests/the-last-harness-context-cap.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Delta-only child message events and unused Pi 0.84 APIs | `extensions/subagents/src/runs/background/subagent-runner.ts`<br>`extensions/subagents/src/runs/shared/subagent-prompt-runtime.ts`<br>`extensions/subagents/test/support/real-session-runner.ts` | `@earendil-works/pi-coding-agent` → `AgentSession`/extension event bus and lane-based session/runtime APIs | Re-reviewed against `0.84.1`; `message_update` remains delta-only and the changed provider-refresh/harness-session APIs are not called by TLH | TLH consumes `assistantMessageEvent` deltas for live child text, drops noisy persisted `message_update` records, and relies on existing session APIs. No speculative migration to provider refresh context, credential synchronization, or lane-based harness/session repositories is needed. | Medium: event payload or session API drift could suppress child output or persist noisy/oversized logs. | On every Pi version bump and subagent runtime change. | Recheck delta parsing/persistence guards, then run `npm run test:subagents`. |

## Practical sync checklist

1. Reconfirm the relevant upstream snapshot first.
2. Diff tracked sources; for generated runtime outputs, review same-named `scripts/**/*.mts` and authoritative `extensions/**/*.ts` sources rather than their generated `scripts/**/*.mjs` / `extensions/**/*.js` mirrors.
3. Preserve TLH-only safety behavior unless the ticket explicitly says to change it.
4. Run the narrowest targeted tests listed above plus a `git diff --check` pass.
5. If provenance is incomplete, say so explicitly in the PR or ticket notes rather than implying stronger attestation.
