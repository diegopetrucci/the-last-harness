# Upstream-sync inventory

This contributor-only inventory tracks TLH code that is either copied/adapted from upstream projects or intentionally shimmed against Pi internals. Use it before editing those areas, especially during upstream version bumps.

## Scope and exclusions

Included here:

- active copied/adapted code called out by ticket `tlhm-nkh9`
- Pi-sensitive compatibility shims that patch or mirror upstream behavior

Explicitly excluded here:

- ordinary dependency pins by themselves
- generated runtime `scripts/**/*.mjs` outputs that mirror same-named `scripts/**/*.mts` sources, plus generated same-layout `extensions/**/*.js` outputs that mirror authoritative `extensions/**/*.ts` sources; see [local development](local-development.md) for the generation/freshness workflow

## Review cadence

- **Pi-sensitive items**: re-review on every `@earendil-works/pi-coding-agent` version bump.
- **`annotate-git-diff`**: re-review at least quarterly and on upstream package/repo releases affecting the review UI/flow. It is not a Pi-cadence row and is intentionally not restamped by the Pi 0.85.1 review below.

## Pi 0.85.1 review evidence

This inventory was re-reviewed against the installed/pinned `@earendil-works/pi-coding-agent` `0.85.1` source and package metadata. Registry metadata reports git head `d981de1229ef899957bbe968bc8dcda02a21f477` and publication on 2026-09-05; `pi-agent-core` (published 2026-09-05T12:04:49Z), `pi-ai` (published 2026-09-05T12:05:47Z), and `pi-tui` (published 2026-09-05T12:05:35Z) all report the same git head. The installed package JSON reports version `0.85.1`, and its distributed source maps retain the authoritative TypeScript source in `sourcesContent`. The 0.85.0/0.85.1 coding-agent changelogs and the installed source maps for coding-agent, agent-core, pi-ai, and pi-tui were reviewed. The 0.85.0 changes relevant to this inventory are: the working indicator moved into the default editor border (#8799), persistent Claude thinking effort, `ctx.cwd` now respected by tools (#8627), and compaction/branch-summary ordering fixes. (`session_compact_failed` and failed-extension factory rollback were introduced in 0.84.3 and are present in 0.84.4; they are not new in 0.85.0.) The 0.85.1 changes relevant to this inventory are: configurable save keybindings in model/thinking selectors (#9149), GPT-6 Astra addition, and SDK import fix. Packaging note: 0.85.1 removed `bundleDependencies` entirely; `pi-ai`, `pi-tui`, and `pi-agent-core` moved from bundled to ordinary runtime dependencies, and `@earendil-works/chord` is a new ordinary runtime dependency; `pi-client`, `pi-protocol`, and `pi-server` are devDependencies of 0.85.1 and are not installed in the package dependency tree; `pi-telemetry` appears transitively, not as a direct dependency of `pi-coding-agent`. A real `npm install -g` with npm 11.19.0 showed 165 packages and `lib/node_modules/@earendil-works/` containing only `pi-coding-agent`; unbundled deps nest under `pi-coding-agent/node_modules`. The `uninstall.sh` RUNTIME_OWNED_TOPLEVEL advisory tripwire (bin, lib, node-compile-cache, marker) is unaffected. The rows below record source findings, not just test outcomes: the listed tests validate TLH compatibility and do not prove upstream source review.

## Copied or adapted code

| Item | Local TLH paths | Upstream repo/package path | Reviewed snapshot | TLH divergences | Risk | Review trigger / cadence | Comparison and validation guidance |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Tokens cache-miss logic | `extensions/the-last-harness/tokens-analyzer.ts` | `@earendil-works/pi-coding-agent` → `src/core/cache-stats.ts` | **Originally attested** in code comment: Pi package `0.80.6`; re-reviewed against installed `0.85.1` source. The 0.84.4→0.85.1 comparison found no change in this file. | Pi 0.85.1 retains the five-minute `CACHE_TTL_MS` reference, 1024-token noise floor, previous-request prompt-token accounting, sticky cache-activity detection, compaction/branch reset, paid-versus-cache-read pricing, `idleMs`, and model-change flag. TLH ports the detector into offline report analysis, accepts its broader usage-field aliases, reports `idleMs` for diagnostics, and does not emit transcript notices. | Medium: report math can drift from Pi if upstream changes miss heuristics or usage fields. | On every Pi version bump. | Compare the local cache-miss block with 0.85.1 `cache-stats.ts` (especially `detectMiss`, `asPreviousRequest`, and `scan`), then run `node --test tests/the-last-harness-tokens-analyzer.test.mjs tests/the-last-harness-tokens-command.test.mjs`. |
| `annotate-git-diff` review flow | `extensions/annotate-git-diff/`<br>`extensions/shared/quiet-glimpse.ts` | `Jonghakseo/pi-extension` → `packages/diff-review` (`@ryan_nookpi/pi-extension-diff-review`), with original inspiration credit to `badlogic/pi-diff-review` | **Historically reconstructed, not originally attested:** upstream commit `2e839268686fbc530a5ab2cef4667d53fdb4b2d5`; `packages/diff-review` tree SHA `bd0701670693372e687ed732e98ec338e60a0fb4`; package version `0.2.5` | TLH renamed the command/package surface, ships it first-party, moved/adapted the quiet Glimpse host into a shared helper, inlines Monaco assets at build time, and has TLH-specific git, prompt, clipboard, window, and watch behavior. TLH also hardens quiet Glimpse protocol-line parsing by silently ignoring valid non-object JSON while preserving valid object-message data. | High: this is a larger adapted codebase with UI, git, process/protocol, and host-integration surface area. | Quarterly and on upstream package/repo releases affecting the review flow; not on Pi version bumps alone. | Compare the upstream package tree at the reconstructed snapshot with the full local derived family, including `extensions/shared/quiet-glimpse.ts`; run `npm run typecheck` and `node --test tests/quiet-glimpse.test.mjs tests/annotate-git-diff-command.test.mjs tests/annotate-git-diff-git.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Package-update notice renderer | `extensions/the-last-harness/package-update-notice.ts` | `@earendil-works/pi-coding-agent` → `src/modes/interactive/interactive-mode.ts` (`showPackageUpdateNotification`) | Re-reviewed against installed `0.85.1`; the method still accepts `string[]`, appends to `chatContainer` via `addChild`, and calls `ui.requestRender()`. | TLH retains its prototype override and replaces Pi's warning-styled package-update banner copy with TLH-specific guidance (`tlh update --extensions`), using the same internal `chatContainer`/`ui` fields and TUI components. Pi's 0.85.0 working-indicator change (#8799) moved the spinner into the default editor border but did not change `chatContainer`, `showPackageUpdateNotification`, or the asynchronous package-check call site; the hook contract is unchanged. | Medium: prototype method stability plus internal renderer field and invocation assumptions can drift on upstream interactive-mode changes. | On every Pi version bump and interactive renderer change. | Compare `showPackageUpdateNotification` and its asynchronous call in 0.85.1 `interactive-mode.ts` before editing the override; run `node --test tests/the-last-harness-extension-package-update-notice.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Model-reference matcher shim | `extensions/the-last-harness/model-visibility.ts` (`findExactModelReferenceMatch`, exact-match patch) | `@earendil-works/pi-coding-agent` → `src/core/model-resolver.ts` (`findExactModelReferenceMatch`), with contracts in `src/modes/interactive/interactive-mode.ts` (`findExactModelMatch`, `getModelCandidates`), `src/core/model-runtime.ts` (`getAvailable`, `getAvailableSnapshot`), and the `src/core/model-registry.ts` compatibility facade | Re-reviewed against installed `0.85.1`. The matcher still trims and prefers a unique canonical provider/model match, then provider-plus-id and unique bare-id matches; GPT-6 Astra was added to the catalog in 0.85.1 as a new OpenAI model but did not change the matching order or resolver contract. | TLH preserves Pi's filtered cached-match priority, then checks the unfiltered cache for an unscoped canonical reference that resolves to a TLH-hidden model, avoiding refresh/UI for known hidden models. After Pi's delegated catalog refresh, TLH retries the unfiltered snapshot so newly discovered hidden canonical models remain resolvable; bare and scoped references remain delegated, while visible, missing, and ambiguous outcomes retain Pi's priority. | High: the private `findExactModelMatch` prototype patch and model-selection contracts can drift with no public extension API guarantee. | On every Pi version bump and model resolver/catalog change. | Compare the local matcher and fallback order with 0.85.1 `model-resolver.ts` and `interactive-mode.ts`; re-check `ModelRuntime` snapshots and `ModelRegistry` compatibility reads, then run `node --test tests/the-last-harness-model-visibility.test.mjs tests/the-last-harness-extension-static.test.mjs`. |

## Pi-sensitive compatibility shims

| Shim | Local TLH paths | Upstream repo/package path | Reviewed snapshot | TLH divergences | Risk | Review trigger / cadence | Comparison and validation guidance |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Prototype overrides | `extensions/the-last-harness/new-version-notice.ts`<br>`extensions/the-last-harness/package-update-notice.ts` | `@earendil-works/pi-coding-agent` → `src/modes/interactive/interactive-mode.ts` | Re-reviewed against installed `0.85.1`; `showNewVersionNotification(release)` and `showPackageUpdateNotification(packages)` remain public prototype methods, with the same chat-container/UI render fields and call-site argument shapes. | TLH retains its no-op `pi update` suppression and package-update rewrite. Pi 0.85.0's working-indicator change (#8799) moved the spinner into the default editor border but did not affect `chatContainer`, the render methods, or their asynchronous call sites; both checks remain asynchronous after initialization, and neither changes the fields consumed by the TLH hooks. | Medium: although both methods appear in the published class declaration, TLH still depends on prototype hook stability plus internal instance-field and invocation assumptions used by the renderer. | On every Pi version bump and interactive renderer change. | Confirm both methods, their asynchronous call sites, and renderer instance fields in 0.85.1 source; run `node --test tests/the-last-harness-extension-new-version-notice.test.mjs tests/the-last-harness-extension-package-update-notice.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Model/effort persistence seam | `extensions/the-last-harness/model-selection-scope.ts`<br>`extensions/the-last-harness/primary-agent-runtime.ts`<br>`extensions/the-last-harness/effort-command.ts` | `@earendil-works/pi-coding-agent` → `src/core/agent-session.ts` (`ModelMutationOptions`, `setModel`, `_emitModelSelect`, `setThinkingLevel`), `src/modes/interactive/interactive-mode.ts` (`showModelSelector`, `showThinkingSelector`), public `src/modes/interactive/components/model-selector.ts` and `thinking-selector.ts`, and `src/core/settings-manager.ts` | Re-reviewed against installed `0.85.1`; Pi's model/thinking mutation and picker APIs are the final explicit-persistence contract. | **Final seam:** Pi's native model picker calls `AgentSession.setModel(model, { persist: false })` for Enter and `{ persist: true }` for the `app.models.save` keybinding, visibly offers Enter/save-key/cancel-key, and uses the public model/thinking components. `AgentSession.setModel` in 0.85.1 checks auth, updates the active model, writes `defaultProvider`/`defaultModel` when `persist` is true, adds the model to the non-empty scope via the private `_addPersistedDefaultToNonEmptyScope` helper (present since at least 0.84.4, unchanged in 0.85.1), clamps thinking for the target model, then awaits `model_select`; `_emitModelSelect` intentionally emits nothing for an unchanged model. TLH does not patch `ModelSelectorComponent`, Pi's model/default setter methods, or the native `/thinking` command. Instead, the isolated runtime wraps only public `AgentSession.setModel` with `AsyncLocalStorage`, carrying the `persist` bit, previous/target model, and an owner token/generation through the complete awaited `model_select` dispatch; `_addPersistedDefaultToNonEmptyScope` runs within the same awaited body and does not change the wrapper's observable async boundary. **Published-build seam:** Pi's package export map exposes the modular root while the bundled Node CLI uses a distinct `dist/bundle/index.js` `AgentSession` constructor; because nested extension `.js` imports can resolve through the modular root, TLH first accepts a valid bundled constructor from Pi's virtual module when available. Otherwise it requires the realpath of the active `dist/bundle/cli.js`, uses `<getPackageDir()>/dist/bundle/index.js` when that entrypoint belongs to the canonical package, and only for a separate file-source/private runtime derives the package root by walking that exact executable layout. The fallback realpath/containment-checks `dist`, `dist/bundle`, `cli.js`, `index.js`, and `package.json`, rejects symlink/path escapes and unexpected layouts, and validates package name/version (`@earendil-works/pi-coding-agent`/`0.85.1`) before importing. TLH installs one owner-marked patch atomically across both prototypes; partial installs roll back and failed TLH factory registration is left to Pi's extension transaction rollback. Unknown/compiled entrypoints do not trigger a second-runtime load, and no cwd, PATH, NODE_PATH, or node_modules search is used. The primary runtime claims that owner-scoped invocation after success; same-model persisted saves use the wrapper's completion callback because Pi emits no event, while failed and session-only calls cannot claim a primary override. `/thinking` remains native and uses Pi's picker and `AgentSession.setThinkingLevel` persistence. `/effort` is only a TLH behavioral alias: it uses exported Pi `ThinkingSelectorComponent`; Enter is session-only, `app.thinking.save` (Ctrl+S by default, configurable since #9149) applies the session level then performs a guarded isolated-profile `defaultThinkingLevel` write with backup/unknown-field preservation, and `tui.select.cancel` (Escape/Ctrl+C) cancels. There are no TLH primary thinking floors or locks. Role defaults, session-only model/thinking retention across turns and tree reapplication, new-session/mode clearing, model capability clamping, and `/reconcile` baseline/override handling remain in `primary-agent-runtime.ts`; Pi cannot identify save-key provenance from provider-auth or other `persist: true` programmatic writes, so TLH treats all successful persisted `setModel` calls as durable compatibility input. Packaging note: the `bundleDependencies` removal in 0.85.1 does not affect the bundle-path seam; `dist/bundle/cli.js` and `dist/bundle/index.js` are still present in the installed package and the version-validation check correctly targets `0.85.1`. | High: the async `AgentSession.setModel` owner seam depends on its `persist` option, active-model update, unchanged-model early return, and awaited extension dispatch; the primary retention and guarded `/effort` write also depend on event ordering and capability metadata. The published bundle artifact path and export must remain present for the bundled Node distribution. | On every Pi version bump and any model-selector, thinking-selector, model-event, settings-persistence, provider-auth, or reasoning-metadata change. | Compare the 0.85.1 `AgentSession.setModel`/`setThinkingLevel` order, native selector callbacks and hints, public component exports, sequential extension dispatch, and modular-versus-bundle constructor identity; run `node --test tests/the-last-harness-model-selection-scope.test.mjs tests/the-last-harness-primary-agent-runtime.test.mjs tests/the-last-harness-effort.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Model visibility filter | `extensions/the-last-harness/model-visibility.ts` | `@earendil-works/pi-coding-agent` → `src/core/model-runtime.ts`, the `src/core/model-registry.ts` compatibility facade, and `src/modes/interactive/interactive-mode.ts` | Re-reviewed against installed `0.85.1`; `getAvailable(providerId?, options?)` retains auth-operation options, `getAvailableSnapshot()` remains the cached read used by interactive lookup, and catalog refresh uses Pi's current runtime orchestration. | TLH still filters async and snapshot availability for user-facing selectors/cycling, preserves original runtime snapshots for internal provider-aware selection, forwards Pi's new/current `getAvailable` options, preserves Pi's filtered cached-match priority, and recovers hidden canonical matches both before and after delegated refresh. The 0.85.1 addition of GPT-6 Astra and removal of Grok Build 0.1 are treated as upstream catalog input, not duplicated in the filter. | Medium: drift here affects model lists, refreshed snapshots, exact matching, and primary-agent model selection. | On every Pi version bump and model catalog/auth change. | Compare against 0.85.1 model-runtime snapshots, the model-registry facade, interactive candidate/lookup behavior, and changed resolver defaults; run `node --test tests/the-last-harness-model-visibility.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Project-trust/resource mirroring | `extensions/the-last-harness/resources.ts` | `@earendil-works/pi-coding-agent` → `src/main.ts`, `src/core/project-trust.ts`, `src/core/resource-loader.ts`, `src/core/settings-manager.ts`, `src/core/package-manager.ts` | Originally mirrored from Pi `0.79.1`; re-reviewed against installed `0.85.1`. | TLH still delegates context loading/trust-sensitive package resolution to Pi. In 0.85.1, `AGENTS.override.md` precedes `AGENTS.md`/`CLAUDE.md`; the trust bootstrap excludes project resources until approval, package/resource precedence remains project-local, project-auto, user-local, user-auto, then package, and project packages win duplicate identities. Pi's 0.84.3 nested `.agents/skills/` discovery and 0.84.4 visible-path glob/BOM handling are inherited. The 0.85.0/0.85.1 changes (ctx.cwd tool fix, compaction ordering) do not affect context-file collection. **Retained TLH divergence (low):** Pi's interactive context summary includes file-backed `SYSTEM.md`/`APPEND_SYSTEM.md` sources through resource-loader accessors; TLH's individual context-file inventory, shown as the expanded `Context:` line, intentionally continues to report only `loadProjectContextFiles()`. Focused coverage proves override precedence and ancestor inheritance. | Medium: trust drift could expose or hide the wrong startup resources. | On every Pi version bump and resource-loader, trust, package-manager, or skill discovery change. | Compare 0.85.1 trust bootstrap, context-file candidates, package precedence, and resource glob behavior against the local collector; run `node --test tests/the-last-harness-resources.test.mjs tests/the-last-harness-header-warning.test.mjs tests/the-last-harness-header-wrap.test.mjs`. |
| Launch-context prompt attribution | `extensions/the-last-harness/launch-context.ts`<br>`extensions/the-last-harness/resources.ts`<br>`extensions/the-last-harness/primary-agent-runtime.ts`<br>`extensions/the-last-harness.ts` | `@earendil-works/pi-coding-agent` → `src/core/system-prompt.ts`, `src/core/agent-session.ts` (`_rebuildSystemPrompt`), `src/core/skills.ts`, `src/core/resource-loader.ts`, and `src/core/extensions/types.ts` (`getActiveTools`, `getAllTools`, `ToolInfo`) | Introduced against `0.84.1`; re-reviewed against installed `0.85.1`. | Pi 0.85.1's `buildSystemPrompt` still joins appended prompt text with blank lines, places context and (when `read` is active) skill XML before the working-directory line, and keeps the custom-prompt working-directory trailing newline. `_rebuildSystemPrompt` still supplies loaded skills/context, active-tool snippets, and trimmed prompt guidelines; `ExtensionRunner.emitBeforeAgentStart` still visits handlers sequentially, passing each handler the current prompt. TLH estimates launch allocation with conservative characters/4 math, shares deterministic `buildActivePrimarySystemPrompt` construction with `before_agent_start`, carves known context/skill/guideline contributions, and counts active `ToolInfo` name/description/parameters definitions without inventing provider overhead. The 0.85.0 persistent-thinking and working-indicator changes do not affect prompt assembly or tool-definition formatting. | Medium: upstream prompt, skill XML, resource, or tool-definition formatting drift can skew the approximate category split without breaking startup. | On every Pi version bump and any system-prompt, resource, skill, or tool-format/API change. | Compare Pi 0.85.1 prompt assembly, `ToolInfo` fields, and sequential before-agent-start composition with the estimator assumptions; run `node --test tests/the-last-harness-launch-context.test.mjs tests/the-last-harness-resources.test.mjs tests/the-last-harness-extension-startup-warning.test.mjs`. |
| Private settings lock | `extensions/the-last-harness/profile-state.ts` (`getSettingsStorageForWrite`, `withLockedTlhSettingsWrite`, `assertSafeTlhSettingsPath`) | `@earendil-works/pi-coding-agent` → `src/core/settings-manager.ts` (`SettingsManager.storage`, `FileSettingsStorage.withLock`) | Re-reviewed against installed `0.85.1`; the private storage property and synchronous `withLock("global", ...)` contract remain present. Settings gained `defaultThinkingLevel` (from 0.84.3) and terminal-capability; the 0.84.4→0.85.1 diff adds no new settings fields; the storage shape and lock contract are unchanged. | TLH continues to layer isolated-profile-only write guards, backups, and symlink/hardlink checks around Pi's storage lock. `/effort` uses this guarded path for its future-session default; no speculative migration to another settings API is needed, and no native model setter is patched. | Medium: the private storage property/lock contract or settings-path layout can change upstream and invalidate TLH's safety assumptions. | On every Pi version bump and settings persistence/permission change. | Recheck 0.85.1 `SettingsManager.storage`, `FileSettingsStorage.withLock`, changed-field merging, and path construction before editing; run `node --test tests/the-last-harness-primary-agent-runtime.test.mjs tests/the-last-harness-effort.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Thinking-level cast | `extensions/the-last-harness/thinking.ts` | `@earendil-works/pi-coding-agent` → extension API thinking-level typings/runtime in `src/core/extensions/types.ts`, `src/core/agent-session.ts`, and `src/main.ts`, with `ThinkingLevel` from `@earendil-works/pi-agent-core` | Re-reviewed against installed `0.85.1`; the public extension API still accepts the seven TLH levels, including legacy `max`, and native `AgentSession.setThinkingLevel` clamps against current model capabilities. The 0.85.0 persistent-thinking feature stores effort per Anthropic transport but does not change the `ThinkingLevel` type contract exposed to extensions. | Legacy compatibility cast retained without behavior change as explicitly required; it remains a separately scoped cleanup candidate. TLH's supported-level filter honors model reasoning metadata, including `thinkingLevelMap`, while primary runtime retention/capability clamping remains outside this cast. | Low: stale compatibility code can obscure the real type contract even though this cast does not change runtime behavior. | Re-evaluate on the next Pi bump or thinking API type change. | Compare the published 0.85.1 `ExtensionAPI.setThinkingLevel` parameter and agent-core `ThinkingLevel` with TLH's `ThinkingLevel`; run `node --test tests/the-last-harness-extension-static.test.mjs tests/the-last-harness-effort.test.mjs`. |
| Subscription usage compatibility facade | `extensions/the-last-harness/subscription-usage.ts`<br>`extensions/the-last-harness/subscription-usage-facade.ts`<br>`extensions/the-last-harness/footer-subscription-usage.ts` | `@earendil-works/pi-coding-agent` → `src/core/model-registry.ts` and `src/core/model-runtime.ts` auth facade | Re-reviewed against installed `0.85.1`; `ModelRuntime` still exposes `isUsingOAuth`, `getAuth`, `getProviderAuthStatus`, and configured-auth checks, while `ModelRegistry` still exposes `isUsingOAuth`, `getApiKeyForProvider`, and `getProviderAuthStatus`; direct `authStorage` remains an optional legacy compatibility shape. GPT-6 Astra was added to the pre-existing OpenAI Codex subscription provider in 0.85.1 as an upstream catalog entry; TLH's facade is unaffected. | TLH intentionally keeps provider-specific usage endpoints, token-fingerprint/account cache isolation, runtime-credential suppression, and optional legacy credential fallback. It uses the published facade and does not adopt Pi's credential-operation/refresh internals merely because 0.85.x added or retained them. | Medium: provider auth shape and subscription eligibility semantics are upstream-sensitive; stale usage must not be shown after credential changes. | On every Pi version bump and provider-auth contract change. | Recheck the published 0.85.1 ModelRegistry/ModelRuntime auth methods and credential synchronization behavior; run `node --test tests/the-last-harness-subscription-usage.test.mjs tests/the-last-harness-extension-usage-refresh.test.mjs tests/the-last-harness-footer-usage.test.mjs`. |
| Context-cap model mutation | `extensions/the-last-harness/context-cap.ts` | `@earendil-works/pi-coding-agent` → `ExtensionContext.model`, `modelRegistry.getAll()`, and `model_select`/`session_start`/`session_shutdown` events; compaction uses model `contextWindow` in `src/core/agent-session.ts` | Re-reviewed against installed `0.85.1`; model identity/context-window and lifecycle event contracts remain usable, and Pi's large-result pre-response compaction does not provide a dedicated cap API. The 0.85.0 persistent-thinking and compaction-ordering fixes do not add a new cap API; the `contextWindow` threshold path is unchanged. | TLH keeps its 200k effective cap as a reversible WeakMap-backed mutation across registry models and the active model, restoring windows on shutdown. It remains intentionally independent of Pi's compaction internals while applying to the model `contextWindow` Pi uses for threshold/overflow decisions. | Medium: model-object identity and lifecycle ordering can drift, leaving a cap applied or restored incorrectly. | On every Pi version bump and context/compaction API change. | Recheck 0.85.1 `ExtensionContext.model`, model-registry identity, session lifecycle events, and `_compactBeforeNextAssistantResponse`/`_checkCompaction`; run `node --test tests/the-last-harness-context-cap.test.mjs tests/the-last-harness-extension-static.test.mjs`. |
| Child message events, supervisor-tool fallback rendering, sendMessage queueing, extension-factory rollback, and unused Pi 0.84–0.85 APIs | `extensions/subagents/src/runs/background/subagent-runner.ts`<br>`extensions/subagents/src/runs/shared/subagent-prompt-runtime.ts`<br>`extensions/subagents/src/supervisor/native-supervisor-channel.ts`<br>`extensions/subagents/src/extension/index.ts`<br>`extensions/subagents/test/support/real-session-runner.ts` | `@earendil-works/pi-coding-agent` → `AgentSession`/extension event bus, `src/modes/json-event.ts`, `src/modes/interactive/components/tool-execution.ts`, and `src/core/extensions/loader.ts`; lane-based session/runtime APIs remain unused | Re-reviewed against installed `0.85.1`; 0.84.3/0.84.4/0.85.0/0.85.1 message, factory, compaction, and renderer contracts are covered by the current source review. | Pi's agent-core emits assistant-only `message_update` deltas with a cumulative `message` snapshot; JSON mode removes partial snapshots while retaining cumulative usage and tool-call id/name (schema unchanged in 0.85.1). TLH consumes final assistant/message events for live child text and drops noisy persisted `message_update` records (including the cumulative-usage wrapper). Pi's `sendCustomMessage` steers when streaming unless `triggerTurn: false`; the latter defers context-only custom messages until the current turn's tool results are persisted, fixing provider message ordering. TLH completion notices call `pi.sendMessage` without options, so they intentionally steer an active parent turn; idle notices use a follow-up nudge. The separately registered child `contact_supervisor` and parent `subagent_supervisor` tools define no custom `renderResult`, so Pi's fallback renderer previews/collapses long results but keeps them expandable; the `subagent` orchestration tool defines a custom `renderResult`, including parent `resume`, `interrupt`, and `status`, and is unaffected. Pi's failed-extension factory now discards pending registrations/subscriptions/default flags; TLH's extension-factory rollback tests verify no TLH registrations remain. Pi's `session_compact_failed`, large-result pre-response compaction, final-turn/queued-follow-up ordering, and resumed child JSONL behavior are treated as lifecycle inputs, not as a reason to migrate to unused lane APIs. The `ctx.cwd` fix (#8627) in 0.85.0 now propagates per-task cwd to built-in tools; TLH children spawn as separate processes with explicit `cwd` set to `step.cwd ?? ctx.cwd`, so no TLH code depended on tools ignoring ctx.cwd. The `bundleDependencies` removal does not affect child spawn (pi is launched via shell path, not require). | Medium: event payload or session API drift could suppress child output or persist noisy/oversized logs, while renderer drift could hide supervisor-tool detail or affect the `subagent` tool's custom display. | On every Pi version bump and subagent runtime change. | Recheck 0.85.1 delta parsing/persistence guards, completion-notice delivery, fallback `contact_supervisor`/`subagent_supervisor` displays, custom `subagent` rendering, failed-factory rollback, large-result compaction, final-turn and queued-follow-up ordering, and resumed JSONL sessions; run `npm run test:subagents`. |
| Activity lifecycle bridge | `extensions/the-last-harness/activity-tracker.ts`<br>`extensions/the-last-harness/activity-reporters.ts`<br>`extensions/shared/tlh-effective-activity.ts` | `@earendil-works/pi-coding-agent` → `src/core/extensions/types.ts` (`session_before_compact`, `session_compact`, `session_compact_failed`, `ui_prompt_start`, `ui_prompt_end`, `agent_end`, `turn_start`, tool events) and `src/core/extensions/runner.ts` (`wrapUIPrompt`/`withUIPrompt`) | Re-reviewed against installed `0.85.1`; `session_compact_failed` is emitted for manual/automatic failure or cancellation with reason/abort/retry metadata, and UI prompt events are emitted for the outer blocking `ctx.ui` prompt while nested calls are depth-counted. These contracts are unchanged in 0.85.0/0.85.1. | TLH clears the matching activity reason on failed/aborted compaction without success retry grace, retains a short grace only after successful `willRetry` compaction, and tracks UI prompt waiting separately from `inProgress`. The effective-activity event includes `waitingForUser`; cmux maps it to `waiting` while Herdr receives its supported blocked representation. Async child liveness/terminal cleanup remains TLH-owned and read-only. | Medium: event names, emission timing, nesting, and compaction retry ordering affect external activity state and notification gating. | On every Pi version bump and any activity, compaction, extension-UI, or subagent lifecycle change. | Compare 0.85.1 event declarations and `ExtensionRunner.withUIPrompt`/AgentSession compaction failure paths with the tracker handlers; run `node --test tests/the-last-harness-activity-tracker.test.mjs tests/the-last-harness-activity-reporters.test.mjs tests/notify-activity-gate.test.mjs`. |

## Practical sync checklist

1. Reconfirm the relevant upstream snapshot and registry metadata first.
2. Diff tracked TypeScript sources; for generated runtime outputs, review same-named `scripts/**/*.mts` and authoritative `extensions/**/*.ts` sources rather than their generated `scripts/**/*.mjs` / `extensions/**/*.js` mirrors.
3. For lifecycle-sensitive rows, verify event ordering and failure/cancellation paths, not only successful output.
4. Preserve TLH-only safety behavior unless the ticket explicitly says to change it.
5. Run the narrowest targeted tests listed above plus a `git diff --check` pass.
6. If provenance is incomplete, say so explicitly in the PR or ticket notes rather than implying stronger attestation.
